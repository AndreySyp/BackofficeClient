<UserControl x:Class="BackofficeClient.Resources.UserControls.CUFD"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BackofficeClient.Resources.UserControls"
	         xmlns:fa="http://schemas.fontawesome.io/icons/"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Style="{StaticResource ResourceKey=CUFD}">
            <Button Command="{Binding ShowCreateWindowCommnad}"
                    ToolTip="Добавить заявку">
                <fa:ImageAwesome Icon="PlusSquare"/>
            </Button>
            <Button Command="{Binding DeleteDataCommnad}"
                    ToolTip="Удалить заявку">
                <fa:ImageAwesome Icon="MinusSquare"/>
            </Button>
            <Button Command="{Binding UpdateDataCommand}"
                    ToolTip="Сохранить изменения">
                <fa:ImageAwesome Icon="FloppyOutline"/>
            </Button>
            <Button Command="{Binding FillingFieldsCommand}"
                    ToolTip="Сбросить изменнеия">
                <fa:ImageAwesome Icon="Dropbox"/>
            </Button>
        </StackPanel>
    </Grid>

</UserControl>
